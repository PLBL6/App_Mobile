import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row'
  },
  icon: {
    height: 25,
    width: 25,
    marginHorizontal: 10,
    marginVertical: 30
  }
})

export default styles